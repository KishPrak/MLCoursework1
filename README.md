Brief set of instructions to run architecture to get model file and run on test set:

1. Clone and move into the repo
2. Run main.py to get the model file (expect to take a few minutes)
3. Move model file into the model folder
4. Run produceResults.py to get the csv file on the test set
